/**
 * This file includes polyfills needed by Angular and is loaded before the app.
 * You can add your own extra polyfills to this file.
 */

import 'zone.js';  // Required for Angular (already in main polyfills, but explicit for tests)

// Optional: Add browser polyfills if needed (e.g., for IE support)
// import 'classlist.js';  // Run `npm install --save classlist.js` if using